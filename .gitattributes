# Normalize line endings; shell scripts must stay LF even on Windows checkouts.
* text=auto eol=lf
*.sh    text eol=lf
bin/*   text eol=lf
*.bats  text eol=lf

# Keep generated/example noise out of language stats and diffs where unhelpful.
examples/** linguist-documentation
docs/**     linguist-documentation
